# Runtime dependencies
pydantic>=2.7,<3
httpx>=0.27,<1          # async HTTP — used to call IBM watsonx / OpenAI-compatible APIs

# API layer (FastAPI integration)
fastapi>=0.111,<1       # HTTP framework; includes Pydantic v2 support
uvicorn>=0.30,<1        # ASGI server — use WITHOUT [standard] on Windows/MSYS2

# Development / test dependencies
pytest>=8.2
pytest-asyncio>=0.23
